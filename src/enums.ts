export enum Role {
  Dev = 'dev',
  Client = 'client'
}

export enum QuestionTypeAlias {
  Number = 'number',
  CheckBox = 'checkBox',
  Email = 'email',
  Phone = 'phone',
  Link = 'link',
  ImageChoice = 'imageChoice',
  ShortText = 'shortText',
  NPS = 'nps',
  Date = 'date',
  DropDown = 'dropDown',
  Matrix = 'matrix',
  Slider = 'slider',
  LongText = 'longText',
  RadioButton = 'radioButton',
  SortList = 'sortList'
}

export enum QuestionTypeKind {
  Basic = 'basic',
  Advanced = 'advanced'
}
